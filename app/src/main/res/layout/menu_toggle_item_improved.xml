<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/toggle_item_height"
    android:background="@drawable/premium_button_primary"
    android:padding="@dimen/spacing_medium"
    android:layout_marginBottom="@dimen/spacing_tiny">

    <!-- Feature Name -->
    <TextView
        android:id="@+id/feature_name"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:text="Feature Name"
        android:textColor="#FFFFFF"
        android:textSize="@dimen/text_size_medium"
        android:gravity="center_vertical"
        android:ellipsize="end"
        android:maxLines="1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/feature_toggle"
        app:layout_constraintHorizontal_bias="0"
        app:layout_constraintHorizontal_chainStyle="spread_inside" />

    <!-- Toggle Switch -->
    <Switch
        android:id="@+id/feature_toggle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:thumb="@drawable/seekbar_thumb"
        android:track="@drawable/seekbar_track"
        android:scaleX="0.8"
        android:scaleY="0.8"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/status_dot"
        app:layout_constraintStart_toEndOf="@id/feature_name" />

    <!-- Status Indicator -->
    <View
        android:id="@+id/status_dot"
        android:layout_width="@dimen/status_dot_size"
        android:layout_height="@dimen/status_dot_size"
        android:background="#808080"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout> 