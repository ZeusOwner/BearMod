<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/slider_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Slider Label"
        android:textColor="@color/premium_text_primary"
        android:textSize="14sp" />

    <SeekBar
        android:id="@+id/slider_seekbar"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:progressDrawable="@drawable/premium_slider_progress"
        android:thumb="@drawable/premium_slider_thumb"
        android:splitTrack="false" />

    <TextView
        android:id="@+id/slider_value"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:text="100"
        android:textColor="@color/premium_text_secondary"
        android:textSize="14sp"
        android:gravity="end" />

</LinearLayout> 